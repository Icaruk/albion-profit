import { createTheme, rem } from "@mantine/core";

export const theme = createTheme({
	spacing: {
		xxxs: rem(6),
		xxs: rem(8),
		xs: rem(10),
		sm: rem(12),
		md: rem(16),
		lg: rem(20),
		xl: rem(32),
	},
});
